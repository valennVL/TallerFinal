# PathFinder Minimal — Backend + Frontend

Repositorio con un MVP para explorar rutas en un grafo:
- Backend: FastAPI + SQLModel (SQLite) + JWT auth
- Frontend: React + Vite

## Estructura
- `backend/` — FastAPI app (puerta de entrada: `backend/app/main.py`)
- `frontend/` — React SPA (Vite)

## Requisitos
- Python 3.10+
- Node.js 18+ y npm
- (opcional) git

## Variables de entorno (copia `backend/.env.example` → `backend/.env` y ajusta)
SQLITE_URL=sqlite:///./app.db
JWT_SECRET= CLAVEGRUPO5
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRES_MINUTES=60
CORS_ORIGINS=http://localhost:5173